<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.temple.imageviewerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/image_viewer_app_os"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/image_viewer_app_os_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImageViewerApp">
        <activity android:name=".DisplayActivity"></activity>
        <activity android:name=".SelectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>